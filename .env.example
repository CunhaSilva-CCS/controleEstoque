# Telemetria (opcional — ativa Sentry em builds de produção)
# Obtenha o DSN em https://sentry.io
SENTRY_DSN=

# Ambiente reportado ao Sentry (production, staging, etc.)
SENTRY_ENVIRONMENT=production

# Habilitar Sentry em desenvolvimento (npm run dev / npm start)
SENTRY_ENABLED=false
